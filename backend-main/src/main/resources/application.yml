spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weathertago
    username: root       # 또는 본인이 사용하는 계정명
    password: dnjswns3011@     # 실제 MySQL 비밀번호 입력
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create      # create, update, validate, none 중 선택
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 8080
